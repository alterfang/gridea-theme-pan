		
		<header id="header" class="site-header" 
		<% if (themeConfig.showFeatureImage && post.feature) { %>
		style="background-image: linear-gradient(rgba(0, 0, 0, 0.2), rgba(0, 0, 0, 0.2)),url(<%= post.feature %>)"
		<% } %>
		>
			<div class="site-branding">
									<h1 class="site-title"><a href="<%= themeConfig.domain %>" rel="home"><%= themeConfig.siteName %></a></h1>
										
					<h2 class="site-description"><%- themeConfig.siteDescription %></h2>
										
							</div>
			<nav id="nav-wrapper">
				<div class="container">
					<div class="nav-toggle">
						<div class="bars">
							<div class="bar"></div>
							<div class="bar"></div>
							<div class="bar"></div>
						</div>
					</div><!-- /nav-toggle -->
					<div class="clear"></div>
					<ul id="" class="dove">
		 <% menus.forEach(function(menu) { %>
     			
<li>
	 
	<a  href="<%= menu.link %>"> <%= menu.name %></a></li>
	
    <% }); %>

</ul>
</li>		
		
</ul>				</div>
			</nav><!-- #navigation -->
						<div class="jingge">
<% ['rss', 'twitter', 'github', 'zhihu', 'facebook','linkedin','telegram','douban','instagram','qq','weibo','youtube'].forEach((item) => { %>
<% if (site.customConfig[item]) { %>			
<a  href="<%= site.customConfig[item] %>" target="_blank" ><i class="iconfont icon-<%= item %>"></i></a>
 <% } %>
    <% }) %>
        </header><!-- #masthead -->